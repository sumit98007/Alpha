{
  "name": "alpha-backend",
  "version": "1.0.0",
  "description": "Secure AI Prompt Optimization Gateway",
  "engines": {
    "node": ">=24"
  },
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "npm run build && node --test test/*.test.js && npm run test:environment",
    "test:environment": "node test-environment.js",
    "check": "npm run test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@google/genai": "^2.10.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.2.0",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  }
}
